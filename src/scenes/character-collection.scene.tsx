import * as React from 'react';
import { AppLayout } from 'layouts';
import { CharacterlCollectionContainer } from 'pods/character-collection';

export const CharacterCollectionScene = () => (
  <AppLayout>
    <CharacterlCollectionContainer />
  </AppLayout>
);
